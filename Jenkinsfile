pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building feature-x branch...'
            }
        }
        stage('Lint') {
            steps {
                echo 'Running linter on feature-x branch...'
            }
        }
    }
}
